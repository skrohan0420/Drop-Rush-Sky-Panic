import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.skrohan.droprushskypanic',
  appName: 'drop-rush-sky-panic',
  webDir: 'dist'
};

export default config;
